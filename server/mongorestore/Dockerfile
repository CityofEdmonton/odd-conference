FROM mongo:latest

ADD data data

ENV MONGORESTORE_DATABASE "dev"
ENV MONGORESTORE_HOST "mongo"
ENV MONGORESTORE_SOURCE "/data"

ENTRYPOINT [ \
  "sh", "-c", \
  "mongorestore --drop --db $MONGORESTORE_DATABASE --host $MONGORESTORE_HOST $MONGORESTORE_SOURCE" \
]
